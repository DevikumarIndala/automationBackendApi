// jest-preset.js
module.exports = {
    preset: '@playwright/test',
    testMatch: ['**/*.spec.js'], // Modify this pattern to match your test file naming convention
    // Add other Jest configuration as needed
  };
  

  